interface EmailVerificationTimerProps {
    onResend: () => void;
}
declare const EmailVerificationTimer: ({ onResend }: EmailVerificationTimerProps) => import("react/jsx-runtime").JSX.Element;
export default EmailVerificationTimer;
