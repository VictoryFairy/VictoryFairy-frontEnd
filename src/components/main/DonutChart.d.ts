import { Record } from "@/types/Record";
interface DonutChartProps {
    record: Record;
}
declare const DonutChart: ({ record }: DonutChartProps) => import("react/jsx-runtime").JSX.Element;
export default DonutChart;
