interface ConfirmpasswordProps {
    password: string;
    email: string;
    setstep: (step: number) => void;
}
declare const Confirmpassword: ({ password, email, setstep, }: ConfirmpasswordProps) => import("react/jsx-runtime").JSX.Element;
export default Confirmpassword;
